;;; setup-bookmarks.el --- Configure Bookmarks+

;;; Commentary:

;;; Code:

(add-to-list 'load-path "~/.emacs.d/custom/bookmark+")

(require 'bookmark+)
                                        ;(global-set-key (kbd "C-x C-f") 'sidebar-open)
                                        ;(global-set-key (kbd "C-x C-a") 'sidebar-buffers-open)

(provide 'setup-bookmarks)
;;; setup-sidebar.el ends here
